# Aeneas extraction status (2026-06-08)

Fourth periodic survey (see [`../AENEAS.md`](../AENEAS.md)). **Toolchain moved, but
no obstruction moved.** The headline change is administrative, not behavioral:
aeneas `main` has now **bumped `charon-pin` past charon #1263** (pin is
`9dd7f23c`, charon `v0.1.212`), so last run's non-standard charon override is
**retired** — this survey was produced by a fully by-the-book `gmake build`. Every
crate verdict is byte-for-byte identical to 2026-06-04: same 10 crates produce a
`.llbc`, same 3 (`field`/`air`/`commit`) charon-stack-overflow on `p3-field` (#9),
same `Not_found` aborts (#10) on `monty-31`/`mds`. Still not usable for a campaign.
One upstream status change: charon **#1266 was closed NOT_PLANNED** (GAT lift
out of scope — confirmed won't-fix), so Issue #2 moves to a known limitation.

## Tool versions

| Tool   | Before (2026-06-04) | After (2026-06-08) | Notes |
| ------ | ------------------- | ------------------ | ----- |
| aeneas | `5c70d9b0`          | `39c6a720`         | branch `main`; 3 commits (charon-pin bump + "add public" #1103) |
| charon | `985e04c6` (v0.1.210, **override**) | `9dd7f23c` (v0.1.212, **pinned**) | now the pin aeneas expects; 5 commits inc. #1267; toolchain `nightly-2026-06-01` |

- aeneas after: `39c6a720` — "add public (#1103)" (prior commit `a2e779f0` = "Merge #1104 update-charon2").
- charon after: `9dd7f23c` — "Add `ConstantExprKind::Call`, for globals".
- Build: `gmake build` (aeneas OCaml) + charon `make build` (Rust binary, since the
  cached binary was last run's `985e04c6`) — **both succeeded**. `check-charon`
  passes cleanly because the local charon commit now equals the pin (no manual
  pin edit, no override). Smoke-tested `util`: charon exit 0, aeneas runs, **no
  "Hash-consing key not found" mismatch** — the by-the-book build is consistent.

## Delta vs previous report (2026-06-04)

- **The charon override is retired — last report's "watch for" item came true.**
  On 2026-06-04 aeneas `main` (`5c70d9b0`) still pinned the pre-#1263 commit
  `a535e914`, forcing us to build charon at `main` (`985e04c6`) and rebuild
  aeneas's OCaml against it. aeneas has since bumped its `charon-pin` to
  `9dd7f23c` (5 commits *ahead* of `985e04c6`, 0 behind → contains #1263). The
  normal `git checkout <pin> && gmake build` path now includes the
  `GenericsMismatch` fix. No special-casing this run or future runs.
- aeneas moved `5c70d9b0` → `39c6a720`: `add public (#1103)`, `Merge #1104
  update-charon2`, `Update charon`. The translator-relevant change is the
  charon bump; #1103 is a visibility tweak. Source line numbers shifted slightly
  (`trait_impl_is_builtin` `Translate.ml:1017`→`1014`;
  `ty_has_mut_borrow_for_region_in_pred` def now `TypesUtils.ml:228`).
- charon moved `985e04c6` → `9dd7f23c`: `Add test`, `Make default method
  duplication optional`, `Normalize item refs more eagerly`, `#1267
  no-dup-default-methods`, `Add ConstantExprKind::Call for globals`. **None of
  these touched the assoc-type-lift recursion** — #9 stack overflow reproduces
  identically (`field` still 755 `Could not compute` warnings, then SIGABRT).
- **No obstruction fixed or regressed; no per-crate verdict changed.** Error/warn
  counts are identical to 2026-06-04 across all 13 crates (util 37, keccak 3,
  maybe-rayon 88, koala-bear 1593, poseidon2 1061, monty-31 1958, challenger
  2364, mds 446, symmetric 1518, merkle-tree 2242).
- **Upstream status change:** charon **#1266** (Issue #2 root cause) was **closed
  NOT_PLANNED** 2026-06-04 — maintainer: `--lift-associated-types` will never lift
  GATs, and aeneas can't model GATs anyway. So #2 is now a confirmed *known
  limitation* (see [`WORKAROUNDS.md`](WORKAROUNDS.md)), not an open ticket.
- #9 and #10 remain **unfiled** (no matching upstream ticket) — still the two
  candidates to file.

## Invocation

`--preset aeneas` is required (else "Hash-consing key not found … serialization
mismatch"). Each crate uses an isolated, per-crate `CARGO_TARGET_DIR` so a cached
`p3-field` cannot mask the clean-build panic. Driven by
[`run-extract.sh`](run-extract.sh). Logs under `/tmp/aeneas-extract/<crate>/`.

## Crate-by-crate results

| Crate         | charon | aeneas errors | aeneas warns | files | lines | Verdict |
| ------------- | ------ | ------------- | ------------ | ----- | ----- | ------- |
| `util`        | OK         | 37   | 23   | 4 | 1537 | Partial — raw pointers/inline-asm, name clash; ends in #7 `Not_found` |
| `keccak`      | OK         | 3    | 1    | 4 | 1183 | **Closest to clean** — region/type-var internal errors |
| `maybe-rayon` | OK         | 88   | 15   | 4 | 961  | `ParallelSlice(Mut)` trait-method lookups fail |
| `koala-bear`  | OK         | 1593 | 705  | 4 | 1918 | Cascade — parent-clause/const/closures + serde; ends #7 |
| `poseidon2`   | OK         | 1061 | 387  | 4 | 1964 | Same cascade pattern as koala-bear |
| `field`       | **PANIC**  | —    | —    | 0 | 0    | **charon stack overflow (#9)** — assoc-type lift on `PackedFieldExtension`/`BinomialExtensionField` |
| `air`         | **PANIC\***| —    | —    | 0 | 0    | charon stack overflow (#9), transitive on `p3-field` |
| `commit`      | **PANIC\***| —    | —    | 0 | 0    | charon stack overflow (#9), transitive on `p3-field` |
| `monty-31`    | OK         | 1958 | 1954 | 0 | 0    | Reaches aeneas → aborts on #10 `Not_found` (no Lean written) |
| `challenger`  | OK         | 2364 | 873  | 4 | 4900 | Partial — cascade |
| `mds`         | OK         | 446  | 444  | 0 | 0    | Reaches aeneas → aborts on #10 `Not_found` (no Lean written) |
| `symmetric`   | OK         | 1518 | 604  | 4 | 3342 | Partial — cascade |
| `merkle-tree` | OK         | 2242 | 1368 | 4 | 6280 | Partial; ends #7 `Not_found` |

`*` = charon stack-overflows while compiling `p3-field` as a dependency. As in the
previous run the overflow fires only for `air`/`commit`;
`monty-31`/`challenger`/`mds`/`symmetric`/`merkle-tree` translate `p3-field` at
low-enough fidelity to clear charon.

charon `Could not compute the value of` (GAT cascade, #2/#3) counts this run:
`field` 755, `merkle-tree` 525, `air` 388, `commit` 357, `monty-31` 350, `mds`
244, `challenger` 168, `symmetric` 93, `koala-bear` 59, `poseidon2` 51 — identical
to 2026-06-04.

## Obstruction catalog

Numbering carried over from 2026-06-04. #1 resolved (charon #1263). No new issues
this run.

### Issue #1 — charon `GenericsMismatch` panic on `p3-field` — ✅ RESOLVED
Fixed by charon [#1263](https://github.com/AeneasVerif/charon/pull/1263) (closed
[#1260](https://github.com/AeneasVerif/charon/issues/1260)). 0 occurrences. See
[`RESOLVED.md`](RESOLVED.md).

### Issue #9 — charon stack overflow lifting associated types on `p3-field` (top blocker)
```
warning: Could not compute the value of Self::Clause0::…::PrimeSubfield … needed to
         update item reference p3_field::packed::packed_traits::PackedField<…>.
   --> field/src/extension/packed_binomial_extension.rs:190 (and binomial_extension.rs:193)
…
thread 'main' (…) has overflowed its stack
fatal runtime error: stack overflow, aborting
error: could not compile `p3-field` (lib)   (signal: 6, SIGABRT)
```
charon `9dd7f23c`, `--lift-associated-types='*'`. Recursion in the associated-type
lift over `p3-field`'s `PackedFieldExtension`/`BinomialExtensionField` impls (same
cyclic `PrimeSubfield`/`Packing` structure as #1/#2). Preceded by 755 (`field`) /
388 (`air`) `Could not compute` warnings. Blocks `field`, `air`, `commit`.
**Unchanged by charon #1267.** This is the #1 blocker.

### Issue #10 — aeneas uncaught `Not_found` in `ty_has_mut_borrow_for_region_in_pred` (NEW last run)
```
Uncaught exception: Not_found
Raised at Stdlib__Map.Make.find in file "map.ml", line 141
Called from Aeneas__TypesUtils.ty_has_mut_borrow_for_region_in_pred.object#visit_TAdt
            in file "llbc/TypesUtils.ml", line 248
Called from Aeneas__TypesUtils.ty_has_mut_borrow_for_region_in_pred
            in file "llbc/TypesUtils.ml", line 260
```
(Function now defined at `TypesUtils.ml:228` after the aeneas bump.) Aborts
`monty-31` and `mds` before any Lean is written (exit 2). A `Map.find` on
type-decl/region info that isn't present — like #7, a missing defensive lookup, at
a different site.

### Issue #2 — trait parent-clause / assoc-const / assoc-type lookups → charon#1266 CLOSED (NOT_PLANNED)
`trait_parent_clause_id`, `trait_const_id`, `trait_type_id` "Could not find" across
koala-bear/poseidon2/challenger/symmetric/merkle-tree. Root cause (RPITIT/GAT lift
failure writing `Error` placeholders) was filed as charon
[#1266](https://github.com/AeneasVerif/charon/issues/1266), **closed NOT_PLANNED
2026-06-04**: GAT lift is out of `--lift-associated-types` scope and aeneas has no
GAT model. Now a known limitation — see [`WORKAROUNDS.md`](WORKAROUNDS.md).

### Issue #3 — generic associated types (GATs)
charon `Could not compute … GAT`; aeneas `Can not extract trait associated types
with parameters`. Same root as #2.

### Issue #4 — raw pointers, inline asm, ptr casts (`util`)
`Aeneas does not yet support dereferencing raw pointers`, `&raw mut`, raw ptr casts,
`InlineAsm` (`prfm pstl1keep`, …). 8 in `util`.

### Issue #5 — stdlib monomorphisation name clashes (`util`)
`Name clash detected … "core.result.Result.Ok"` (1 in `util`).

### Issue #6 — mixed-recursive declaration groups
`Mixed declaration groups … are not supported yet` — 1 each in koala-bear,
poseidon2, monty-31, challenger, mds, symmetric, merkle-tree.

### Issue #7 — `Not_found` from `trait_impl_is_builtin` → FILED aeneas#1102 (OPEN)
`Translate.ml:1014` (was 1017). Aborts `util`, `koala-bear`, `merkle-tree` (exit 2).
aeneas [#1102](https://github.com/AeneasVerif/aeneas/issues/1102).

### Issue #8 — closures / lambdas don't extract
`closure` + internal error building the closure env: `merkle-tree` 3317,
`monty-31` 1283, `challenger` 1081, `poseidon2` 164, `mds` 271.

## Upstream cross-reference (2026-06-08)

Re-verified via `gh`.

| Local issue | Status upstream | Tickets |
| --- | --- | --- |
| #1 charon `GenericsMismatch` panic | **RESOLVED** | charon [#1260](https://github.com/AeneasVerif/charon/issues/1260) CLOSED by [#1263](https://github.com/AeneasVerif/charon/pull/1263) (MERGED) |
| #2 parent-clause/const/type lookups (RPITIT/GAT root) | **Closed NOT_PLANNED (known limitation)** | charon [#1266](https://github.com/AeneasVerif/charon/issues/1266) CLOSED; related aeneas [#794](https://github.com/AeneasVerif/aeneas/issues/794), [#1051](https://github.com/AeneasVerif/aeneas/issues/1051) |
| #3 GATs | Known limitation (subsumed by #2) | charon [#1078](https://github.com/AeneasVerif/charon/issues/1078), [#534](https://github.com/AeneasVerif/charon/issues/534) |
| #4 raw pointers / asm / ptr casts | Known limitation | charon [#1041](https://github.com/AeneasVerif/charon/issues/1041) |
| #5 stdlib name clashes | Well represented | aeneas [#922](https://github.com/AeneasVerif/aeneas/issues/922), [#767](https://github.com/AeneasVerif/aeneas/issues/767), [#1046](https://github.com/AeneasVerif/aeneas/issues/1046), [#960](https://github.com/AeneasVerif/aeneas/issues/960) |
| #6 mixed-recursive declaration groups | Represented | aeneas [#176](https://github.com/AeneasVerif/aeneas/issues/176) |
| #7 `Not_found` in `trait_impl_is_builtin` | **Represented (ours, OPEN)** | aeneas [#1102](https://github.com/AeneasVerif/aeneas/issues/1102) |
| #8 closures fail to extract | Represented | aeneas [#924](https://github.com/AeneasVerif/aeneas/issues/924), [#891](https://github.com/AeneasVerif/aeneas/issues/891), [#804](https://github.com/AeneasVerif/aeneas/issues/804) |
| #9 charon stack overflow (assoc-type lift) | **Not represented — file it** | none (generic tracking charon [#145](https://github.com/AeneasVerif/charon/issues/145)) |
| #10 aeneas `Not_found` in `ty_has_mut_borrow_for_region_in_pred` | **Not represented — file it** | none |

## Obstructions with no upstream issue/PR

Candidates to file, highest blocker first:

1. **charon: stack overflow lifting associated types on `p3-field`'s
   `PackedFieldExtension`/`BinomialExtensionField` impls** (Issue #9). The #1
   blocker; blocks `field`/`air`/`commit`. Survived charon #1267. *Repro:*
   `cd field && CARGO_TARGET_DIR=/tmp/cf charon cargo --preset aeneas --dest-file
   /tmp/cf/p3_field.llbc`. **File on charon.** Draft:
   [`issue-drafts/01-charon-stack-overflow-assoc-type-lift.md`](issue-drafts/01-charon-stack-overflow-assoc-type-lift.md).
2. **aeneas: uncaught `Not_found` from `Map.find` in
   `ty_has_mut_borrow_for_region_in_pred` (`TypesUtils.ml:228`/`:248`)** (Issue
   #10). Aborts `monty-31`/`mds`. Same shape as #1102 (needs defensive
   `find_opt`), different site. **File on aeneas.** Draft:
   [`issue-drafts/02-aeneas-not-found-ty-has-mut-borrow.md`](issue-drafts/02-aeneas-not-found-ty-has-mut-borrow.md).

## Recommendation

**Stable run — the toolchain advanced but the obstruction landscape did not.** The
one concrete win is hygienic: the charon override is gone, so future surveys are a
plain `gmake build`. Otherwise:

1. **File #9 (charon stack overflow)** — it is the standing #1 blocker and the
   five charon commits since last run (incl. #1267) did not touch it. Now clearly
   distinct from #1266 (which is a deliberate non-goal), so worth its own ticket.
2. **File #10 (aeneas `Not_found`)** — cheap defensive `find_opt`, like #1102.
3. The downstream cascade (#2 parent-clause lookups — now a known limitation, #8
   closures) is the bulk of remaining errors and will dominate once #9/#10 clear.
4. #2/#1266 is settled as a won't-fix: full crypto-core extraction via the vanilla
   `--preset aeneas` is **not** on the upstream roadmap. Scoped, GAT-free
   extraction (`--exclude RawDataSerializable`, leaf-crates-first) remains the
   viable path — see [`WORKAROUNDS.md`](WORKAROUNDS.md).
5. `keccak` remains closest-to-clean (3 errors, no Plonky3 deps).

The hax workspace at `koala-bear/proofs/lean/extraction/` remains the working
verification target.
