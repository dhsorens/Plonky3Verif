# Aeneas extraction status (2026-06-25)

Sixth periodic survey (see [`../AENEAS.md`](../AENEAS.md)). **A quiet run — no
obstruction moved.** aeneas advanced **12 commits** (`8dd8bfb3` → `21e39de9`) but
the whole delta is Lean-backend / tooling work (iterator transformer models,
`translation.json` emission, step-tactic fixes, relative-path cleanup); **none
touched the extraction front-end.** The charon pin **did not move** (`6f058254`,
v0.1.216), so the #1 hard blocker — charon stack-overflows (SIGABRT) lifting
associated types on `p3-field` (#9) — **reproduces identically**, still blocking
`field`/`air`/`commit`. Every per-crate error/warn count and the GAT-warning
cascade are **unchanged**; the only movement is a few-percent drop in emitted
Lean lines on the three partial crates (builtin-extraction churn in the Lean
backend). The one substantive change is upstream, not local: last run's
top-priority filing — the uncaught `CFailure` in method-signature translation
(#11) — **now has a matching open ticket, aeneas
[#1164](https://github.com/AeneasVerif/aeneas/issues/1164)** (opened 2026-06-24,
a `Try`-trait-bound reproducer hitting the exact same crash site). So #11 moves
**Not represented → Represented**, leaving only #9 with no upstream ticket. Build
note: aeneas now needs a **new opam dependency, `ppx_deriving_yojson`** (pulled in
by the `translation.json` work). Still not usable for a campaign.

## Tool versions

| Tool   | Before (2026-06-16) | After (2026-06-25) | Notes |
| ------ | ------------------- | ------------------ | ----- |
| aeneas | `8dd8bfb3`          | `21e39de9`         | branch `main`; **12 commits** — Lean iterator models (#1143), `EmitJson`/`translation.json` (#1009), `bv_decide`/`BvEnumToBitVec` (#1150), `step` tactic fixes (#1047/#1054/#1045/#1135), `#decompose` fixes (#1157/#1159), relative paths (#1161) |
| charon | `6f058254` (v0.1.216) | `6f058254` (v0.1.216) | **unchanged** — `charon-pin` did not move; toolchain `nightly-2026-06-01` |

- aeneas after: `21e39de9` — "update for relative paths and clean (#1161)".
- charon after: `6f058254` — "Update `ItemSource` to reflect the changes" (pin unchanged).
- Build: `gmake build` — **succeeded, but only after installing a new dependency.**
  The `translation.json` work (#1009, `src/EmitJson.ml`) added a hard dependency on
  **`ppx_deriving_yojson`**, which was absent from the `aeneas` opam switch; the
  build failed with `Library "ppx_deriving_yojson" not found` until
  `opam install ppx_deriving_yojson` (3.10.0). As before, the build must run under
  the `aeneas` opam switch (`export OPAMSWITCH=aeneas && eval $(opam env)`; the
  default shell switch `sail` lacks the ppx/visitors deps). `check-charon` passes
  (local charon == pin); **no charon rebuild** (pin unchanged), no override.

## Delta vs previous report (2026-06-16)

- **aeneas `8dd8bfb3` → `21e39de9` (12 commits).** Entirely Lean-backend and
  tooling: `fa699427` "Add models of iterator transformers" (#1143) + `IterAdapters`,
  `1435bdb0` "Emit translation.json" (#1009, new `src/EmitJson.ml`), `3bc3ab5f`
  `#define_bv_decide_ToBitVec`/`BvEnumToBitVec` (#1150), the `step`/`inferPost`
  tactic fixes (`def42472`/`227b0b72`/`5e1856fa`/`cad6a855`), `#decompose` fixes
  (`34d3b272`/`049723cc`), `71f4b158` WHNF reduction, `21e39de9` relative paths.
  **None touch the symbolic→pure / charon-lifting front-end** that the obstructions
  live in — so, as expected, no obstruction moved.
- **charon unchanged.** `charon-pin` still points at `6f058254` (v0.1.216); the
  charon binary was not rebuilt. **#9 reproduces byte-for-byte** (same stack
  overflow at `packed_binomial_extension.rs:190`).
- **No obstruction fixed, none regressed.** Every crate's aeneas error and warning
  counts are **identical** to 2026-06-16 (util 5/0, keccak 3/1, maybe-rayon 14/13,
  koala-bear 789/484, poseidon2 203/150, monty-31 805/800, challenger 884/622,
  mds 112/109, symmetric 184/177, merkle-tree 536/531). The charon GAT cascade
  (`Could not compute the value of`) is **identical** in all ten crates that
  produce it (field 668, air 168, challenger 121, merkle-tree 95, mds 82,
  commit 53, monty-31 47, symmetric 18, koala-bear 6, poseidon2 4).
- **Only measurable change: emitted Lean shrank slightly** on the three partial
  crates — koala-bear 1931 → **1914**, poseidon2 1959 → **1929**, challenger
  4748 → **4607** lines (keccak 1183 and maybe-rayon 930 unchanged). This tracks
  the heavy churn in `extract/ExtractBuiltinLean.ml` (522 lines changed) + the new
  iterator models, i.e. how builtins are rendered — not a coverage regression.
- **#11 now Represented upstream (aeneas [#1164](https://github.com/AeneasVerif/aeneas/issues/1164)).**
  Last run #11 (uncaught `CFailure` in method-signature translation) had *no*
  ticket and was the top filing candidate. On 2026-06-24 someone filed #1164,
  "Crash on `Try` trait bound", whose backtrace is the **exact same crash site** —
  `translate_fun_sigs` → `translate_crate_to_pure.translate_method_sig`
  (`SymbolicToPureTypes.ml` / `Translate.ml`) raising an uncaught
  `Aeneas.Errors.CFailure` to `Main.ml`. Our crates reach it from different
  unsupported types (p3-field assoc-types/closures) rather than a `Try` bound, but
  the bug — a `CFailure` escaping the per-item error-recovery boundary in
  method-sig translation — is the same. #11 ⇒ **Represented**, no longer a filing
  candidate.
- **#7 unchanged, line drift only.** koala-bear still aborts (after emitting partial
  Lean) on the `trait_impl_is_builtin` `Not_found`, now at `Translate.ml:1050-1051`
  (was 1047). Still aeneas [#1102](https://github.com/AeneasVerif/aeneas/issues/1102), OPEN.

## Invocation

`--preset aeneas` is required (else "Hash-consing key not found … serialization
mismatch"). Each crate uses an isolated, per-crate `CARGO_TARGET_DIR` so a cached
`p3-field` cannot mask the clean-build panic. Driven by
[`run-extract.sh`](run-extract.sh). Logs under `/tmp/aeneas-extract/<crate>/`.

## Crate-by-crate results

| Crate         | charon | aeneas errors | aeneas warns | files | lines | Verdict |
| ------------- | ------ | ------------- | ------------ | ----- | ----- | ------- |
| `util`        | OK         | 5    | 0    | 0 | 0    | InlineAsm/Unimplemented, then #11 `CFailure` aborts before Lean (unchanged) |
| `keccak`      | OK         | 3    | 1    | 4 | 1183 | **Closest to clean** — region/type-var internal errors (unchanged) |
| `maybe-rayon` | OK         | 14   | 13   | 4 | 930  | `ParallelSlice` internal errors; emits Lean (unchanged) |
| `koala-bear`  | OK         | 789  | 484  | 4 | 1914 | Cascade; emits Lean then aborts on #7 `Not_found` (lines 1931→1914) |
| `poseidon2`   | OK         | 203  | 150  | 4 | 1929 | Same cascade pattern; emits Lean (lines 1959→1929) |
| `field`       | **PANIC**  | —    | —    | 0 | 0    | **charon stack overflow (#9)** — assoc-type lift, unchanged |
| `air`         | **PANIC\***| —    | —    | 0 | 0    | charon stack overflow (#9), transitive on `p3-field` |
| `commit`      | **PANIC\***| —    | —    | 0 | 0    | charon stack overflow (#9), transitive on `p3-field` |
| `monty-31`    | OK         | 805  | 800  | 0 | 0    | #11 `CFailure` (sub-path B) — aborts before Lean (unchanged) |
| `challenger`  | OK         | 884  | 622  | 4 | 4607 | Partial — cascade, emits Lean (lines 4748→4607) |
| `mds`         | OK         | 112  | 109  | 0 | 0    | #11 `CFailure` (sub-path B) — aborts before Lean (unchanged) |
| `symmetric`   | OK         | 184  | 177  | 0 | 0    | #11 `CFailure` aborts before Lean (unchanged) |
| `merkle-tree` | OK         | 536  | 531  | 0 | 0    | #11 `CFailure` aborts before Lean (unchanged) |

`*` = charon stack-overflows while compiling `p3-field` as a dependency. As before
the overflow fires only for `air`/`commit`; the other `p3-field`-dependent crates
translate it at low-enough fidelity to clear charon.

The dominant aeneas error remains `[Error] Internal error` on `p3-field`'s
`RawDataSerializable::into_byte_stream` / `into_parallel_byte_streams` closures
(monty-31 794, koala-bear 726, challenger 621, merkle-tree 530, symmetric 174,
poseidon2 140, mds 110) — tracked by aeneas
[#1119](https://github.com/AeneasVerif/aeneas/issues/1119) (Issue #8).

## Obstruction catalog

Numbering carried over from 2026-06-16. No new obstruction this run. Status change:
**#11** gained an upstream ticket (aeneas #1164) and is now Represented.

### Issue #9 — charon stack overflow lifting associated types on `p3-field` (top blocker)
```
warning: Could not compute the value of Self::Clause0::…::PrimeSubfield … needed to
         update item reference p3_field::packed::packed_traits::PackedField<…>.
   --> field/src/extension/packed_binomial_extension.rs:190:1
…
thread 'main' (…) has overflowed its stack
fatal runtime error: stack overflow, aborting
error: could not compile `p3-field` (lib)   (signal: 6, SIGABRT)
```
charon `6f058254`, `--lift-associated-types='*'`. Unbounded recursion in the
associated-type lift over `p3-field`'s `PackedFieldExtension`/`BinomialExtensionField`
impls (cyclic `PrimeSubfield`/`Packing`). Preceded by 668 (`field`) `Could not
compute` warnings. Blocks `field`/`air`/`commit`. **Unchanged** (charon pin did not
move). Still the #1 blocker and the only obstruction with **no upstream ticket**.

### Issue #11 — aeneas uncaught `CFailure` in trait method-signature translation → aeneas#1164 (now Represented)
```
Uncaught exception:
  Aeneas.Errors.CFailure(_)
Called from Aeneas__SymbolicToPureTypes.translate_fwd_ty in file
            "symbolic/SymbolicToPureTypes.ml", lines 396-398
… translate_inst_fun_sig_to_decomposed_fun_type:784 / translate_fun_sigs:1031-1032 …
Called from Aeneas__Translate.translate_crate_to_pure.translate_method_sig
            in file "Translate.ml", lines 399-400
Called from Aeneas__Translate.translate_crate in file "Translate.ml", line 2175
Called from Dune__exe__Main in file "Main.ml", lines 743-744
```
A `CFailure` raised in method-signature translation escapes the error-recovery
harness and aborts the whole run (exit 2) **before any Lean is written**. Two
sub-paths: `translate_fwd_ty`/`translate_fun_sigs` (util, symmetric, merkle-tree)
and `TypesAnalysis.analyze_full_ty`/`compute_back_ty_num_levels` (monty-31, mds).
**Now matched by aeneas [#1164](https://github.com/AeneasVerif/aeneas/issues/1164)**
("Crash on `Try` trait bound", opened 2026-06-24): identical
`translate_fun_sigs` → `translate_method_sig` → `translate_crate` → `Main.ml`
crash site (#1164's reproducer is a `Try<Output=B>` method bound; ours fire on
p3-field assoc-types/closures, but the uncaught-`CFailure`-instead-of-graceful-
recovery bug is the same). Line numbers are unchanged from 2026-06-16 (the 12
aeneas commits didn't touch this path; only `Main.ml` shifted 738→743 from the
EmitJson insertion).

### Issue #8 — closures / lambdas don't extract → aeneas#1119
Dominant remaining error: `[Error] Internal error` on `p3-field`'s
`RawDataSerializable::into_byte_stream` / `into_parallel_byte_streams` closures
(monty-31 794, koala-bear 726, challenger 621, merkle-tree 530, symmetric 174).
Tracked by aeneas [#1119](https://github.com/AeneasVerif/aeneas/issues/1119),
[#924](https://github.com/AeneasVerif/aeneas/issues/924),
[#891](https://github.com/AeneasVerif/aeneas/issues/891),
[#804](https://github.com/AeneasVerif/aeneas/issues/804).

### Issue #7 — `Not_found` from `trait_impl_is_builtin` → aeneas#1102 (OPEN)
`Translate.ml:1050-1051` (was 1047). Aborts `koala-bear` after partial Lean
(`util`/`merkle-tree` abort earlier on #11). aeneas
[#1102](https://github.com/AeneasVerif/aeneas/issues/1102).

### Issue #4 — raw pointers, inline asm, ptr casts (`util`, `monty-31`)
`unsupported statement: InlineAsm ("prfm pstl1keep …")`, `Unimplemented` on
`core/src/str/pattern.rs`. charon
[#1041](https://github.com/AeneasVerif/charon/issues/1041).

### Issue #6 — mixed-recursive declaration groups
`Mixed declaration groups … are not supported yet` — 1 each in monty-31,
symmetric, merkle-tree, challenger (others abort before reaching it). aeneas
[#176](https://github.com/AeneasVerif/aeneas/issues/176).

### Issue #5 — stdlib monomorphisation name clashes — not observed this run
`util` aborts on #11 after only 5 errors before reaching it. Ticket family still
open: aeneas [#922](https://github.com/AeneasVerif/aeneas/issues/922),
[#1046](https://github.com/AeneasVerif/aeneas/issues/1046).

### Issue #2 — trait parent-clause / assoc-const / assoc-type lookups — ✅ CASCADE GONE (since 2026-06-16)
`trait_const_id`/`trait_parent_clause_id`/`trait_type_id` "Could not find" remains
**0** in all crates. charon [#1266](https://github.com/AeneasVerif/charon/issues/1266)
remains the NOT_PLANNED root for the GAT-lift non-goal; see
[`WORKAROUNDS.md`](WORKAROUNDS.md).

### Issue #10 — aeneas `Not_found` in `ty_has_mut_borrow_for_region_in_pred` — ✅ NO LONGER REPRODUCES (since 2026-06-16)
Token absent from all logs; `monty-31`/`mds` abort via #11 instead.

### Issue #3 — generic associated types (GATs)
Same root as #2/#9; charon [#1078](https://github.com/AeneasVerif/charon/issues/1078),
[#534](https://github.com/AeneasVerif/charon/issues/534).

## Upstream cross-reference (2026-06-25)

Re-verified via `gh` (all states confirmed this run).

| Local issue | Status upstream | Tickets |
| --- | --- | --- |
| #9 charon stack overflow (assoc-type lift) | **Not represented — file it** | none (generic tracking charon [#145](https://github.com/AeneasVerif/charon/issues/145) OPEN) |
| #11 aeneas uncaught `CFailure` in method-sig translation | **Represented (NEW this run)** | aeneas [#1164](https://github.com/AeneasVerif/aeneas/issues/1164) OPEN (opened 2026-06-24) |
| #8 closures fail to extract | Represented | aeneas [#1119](https://github.com/AeneasVerif/aeneas/issues/1119), [#924](https://github.com/AeneasVerif/aeneas/issues/924), [#891](https://github.com/AeneasVerif/aeneas/issues/891), [#804](https://github.com/AeneasVerif/aeneas/issues/804) |
| #7 `Not_found` in `trait_impl_is_builtin` | Represented (ours, OPEN) | aeneas [#1102](https://github.com/AeneasVerif/aeneas/issues/1102) |
| #6 mixed-recursive declaration groups | Represented | aeneas [#176](https://github.com/AeneasVerif/aeneas/issues/176) |
| #5 stdlib name clashes (not observed this run) | Represented | aeneas [#922](https://github.com/AeneasVerif/aeneas/issues/922), [#1046](https://github.com/AeneasVerif/aeneas/issues/1046) |
| #4 raw pointers / asm / ptr casts | Known limitation | charon [#1041](https://github.com/AeneasVerif/charon/issues/1041) |
| #3 GATs | Known limitation (subsumed by #2) | charon [#1078](https://github.com/AeneasVerif/charon/issues/1078), [#534](https://github.com/AeneasVerif/charon/issues/534) |
| #2 parent-clause/const/type lookups | Cascade gone (root #1266 NOT_PLANNED) | charon [#1266](https://github.com/AeneasVerif/charon/issues/1266) CLOSED |
| #10 aeneas `Not_found` in `ty_has_mut_borrow…` | No longer reproduces (superseded by #11) | — |
| #1 charon `GenericsMismatch` panic | RESOLVED (charon #1263) | see [`RESOLVED.md`](RESOLVED.md) |

## Obstructions with no upstream issue/PR

Only one remains this run (down from two — #11 was filed upstream as #1164):

1. **charon: stack overflow lifting associated types on `p3-field`'s
   `PackedFieldExtension`/`BinomialExtensionField` impls** (Issue #9). The #1
   blocker; blocks `field`/`air`/`commit`; unchanged across the last several
   surveys. *Repro:* `cd field && CARGO_TARGET_DIR=/tmp/cf charon cargo
   --preset aeneas --dest-file /tmp/cf/p3_field.llbc`. **File on charon.** Draft:
   [`issue-drafts/01-charon-stack-overflow-assoc-type-lift.md`](issue-drafts/01-charon-stack-overflow-assoc-type-lift.md).

## Recommendation

**Holding pattern.** The 12 aeneas commits were all backend/tooling (iterator
models, `translation.json`, tactics) and the charon pin didn't move, so nothing in
the extraction obstruction landscape changed. Net actions:

1. **File #9 (charon stack overflow)** — now the *only* unrepresented blocker, and
   the standing #1 hard blocker for `field`/`air`/`commit`. It has survived every
   charon bump since 2026-06-04. This is the single highest-value filing.
2. **Track / engage on aeneas #1164** — it now covers #11 (the method-sig
   `CFailure` regression). Worth adding a comment that the same crash fires on
   non-`Try` inputs (p3-field assoc-types) so the fix targets the recovery boundary,
   not just the `Try` bound. A fix here would restore partial Lean for
   `util`/`symmetric`/`merkle-tree`.
3. **Watch aeneas #1119** — still tracks the dominant remaining error (closures
   borrowing their env via `RawDataSerializable`). Once #9/#11 clear, this and the
   closure family (#8) will dominate.
4. **Build hygiene:** the `aeneas` opam switch now needs `ppx_deriving_yojson`
   (added by the `translation.json` work, #1009). Future rebuilds on a fresh switch
   must install it.
5. Closest-to-clean unchanged: `keccak` (3 errors, no Plonky3 deps); `maybe-rayon`
   (14) next; among `p3-field` dependents, `poseidon2` (203) is the lowest-error
   crate that still emits Lean. The viable path is still GAT-free, leaf-crate-first,
   scoped extraction (`--exclude RawDataSerializable`) — see
   [`WORKAROUNDS.md`](WORKAROUNDS.md).

The hax workspace at `koala-bear/proofs/lean/extraction/` remains the working
verification target.
</content>
