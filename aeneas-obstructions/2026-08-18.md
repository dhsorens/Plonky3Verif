# Aeneas extraction status (2026-08-18)

Ninth periodic survey (see [`../AENEAS.md`](../AENEAS.md)). **A true null-delta
run — and therefore a full reproducibility control.** The toolchain did not move
at all (`git pull` on aeneas: *Already up to date*; the charon pin is unchanged;
**0 commits** on both sides), and the Plonky3 source did not move either (HEAD
is still `c095c435`, the commit of the previous survey — the working tree only
carries uncommitted hax-pipeline files, which are outside the sample). The
result is exactly what determinism predicts: **every crate reproduces the
2026-08-06 numbers bit-for-bit in every column** — charon exits, charon type
errors, aeneas error/warn counts, Lean file and line counts, and all obstruction
signatures at identical source locations, OCaml line numbers, and occurrence
counts. Unlike the 2026-08-06 run (where the source had synced with upstream),
there is no confound this time: this run pins the entire baseline.

The only movement is upstream *bookkeeping*: a wave of new third-party aeneas
issues (#1247–#1266) landed since 2026-08-06. None changes a classification,
but aeneas [#1264](https://github.com/AeneasVerif/aeneas/issues/1264) is a
**second partial match for our #6** (same "mixed mutually recursive
definitions" error, fun × trait-impl flavor vs our trait × closure-type), and
the sheer volume of fresh reports in aeneas's tracker is a reminder that our
four unfiled obstructions (#9, #16, #13, #14) are competing for attention we
haven't asked for. **Still not usable for a campaign.**

## Tool versions

| Tool   | Before (2026-08-06) | After (2026-08-18) | Notes |
| ------ | ------------------- | ------------------ | ----- |
| aeneas | `daa85d7e`          | `daa85d7e`         | **0 commits**, branch `main`; `git pull --ff-only` fetched only nightly tags |
| charon | `340b1af4`          | `340b1af4`         | **v0.1.232**, pinned by aeneas (`charon-pin` unchanged, checkout matches) |

Build: `gmake build` — **succeeded** (a no-op rebuild; binaries already
current). rustc: `nightly-2026-06-01` (charon's pinned toolchain). Platform:
Darwin 25.5.0 arm64.

## Delta vs previous report (2026-08-06)

**Net: nothing moved — toolchain, source, and results are all identical.**

- aeneas `daa85d7e` → `daa85d7e`, charon `340b1af4` → `340b1af4` (0 commits
  each). Plonky3 HEAD `c095c435` → `c095c435` (0 commits; last survey's
  upstream-sync confound is absent this run).
- **Per-crate results: bit-identical in every column** (see table). Verified
  beyond the raw counts: #9's GAT-warning counts (`field` 17, `air` 289,
  `commit` 43) and stack-overflow abort (`signal: 6, SIGABRT`, no backtrace);
  #11's three sub-paths at unchanged line numbers (**A**
  `translate_fwd_ty:396-398` in `util`/`merkle-tree`, **B**
  `TypesAnalysis.ml:372` in `monty-31`/`mds`, **C**
  `translate_fun_sigs:1012-1013` in `symmetric`); #7 at
  `Translate.ml:1073-1074`; #16's type-error counts (`symmetric` 49,
  `monty-31` 9, `merkle-tree` 8, `koala-bear` 2, all with charon exit 0); #14
  at `keccak/src/lib.rs:121:8-123:9`; #13 (`poseidon2` 6, `monty-31` 2); #12
  (`merkle-tree` 36, `challenger` 8); #6's cascade dominance (749 signature
  failures in `monty-31`); #3 at `SymbolicToPure.ml:222` (4/4/3); #8 at
  `SymbolicToPure.ml:146` (6/7/7/18/19/20).
- **Upstream ticket-state changes: none.** All 14 tracked aeneas tickets and
  all tracked charon tickets re-verified via `gh` — every OPEN is still OPEN,
  charon #1266/#1041 still CLOSED.
- **New upstream issues since 2026-08-06** (third-party, none in our binaries):
  aeneas #1247–#1266 (a dozen bug reports: checked binops #1257, `u8 as char`
  #1251, indexed-enum match #1253, `const _` identifiers #1250, `Option<&T>::
  as_ref` lifetimes #1252, missing `From<bool>` Lean instances #1259, closure
  returning reference #1266, async request #1265, …). Relevant to us:
  **aeneas [#1264](https://github.com/AeneasVerif/aeneas/issues/1264)** —
  mutually recursive functions × trait impls, the same
  `Detected groups of mixed mutually recursive definitions` error as our #6
  (different group flavor) — added to the #6 row as a second partial match
  alongside #176/#1241. charon gained only #1348 (pre/postcondition attributes
  feature request, PR #1349) — not ours.
- **Per-crate verdict changes: none.**

## Invocation

`--preset aeneas` is required (else "Hash-consing key not found … serialization
mismatch"). Each crate uses an isolated, per-crate `CARGO_TARGET_DIR` so a
cached `p3-field` cannot mask the clean-build panic. Driven by
[`run-extract.sh`](run-extract.sh). Logs under `/tmp/aeneas-extract/<crate>/`.

## Crate-by-crate results

Identical to 2026-08-06, every column.

| Crate         | charon | aeneas errors | aeneas warns | files | lines | Verdict |
| ------------- | ------ | ------------- | ------------ | ----- | ----- | ------- |
| `maybe-rayon` | OK         | **0**  | 5   | 4 | 985  | **✅ CLEAN — exit 0.** 5 warns only (#15 missing Lean `Iterator` models) |
| `keccak`      | OK         | **2**  | 0   | 4 | 1175 | **Closest to clean** — 1 unique error (#14 `type_var_id: 0`) |
| `util`        | OK         | 5      | 0   | 0 | 0    | InlineAsm/`Unimplemented` (#4), then #11 `CFailure` (A) aborts before Lean |
| `koala-bear`  | OK(2 type errors) | 840 | 502 | 4 | 1898 | #6/#3 cascade + #16; emits Lean, then aborts on #7 `Not_found` |
| `poseidon2`   | OK         | 180    | 134 | 4 | 1742 | #6 cascade + #13 arrow types (6); emits Lean, exit 1 |
| `challenger`  | OK         | 756    | 496 | 4 | 4165 | #6 cascade + #12 HRTB (8); emits Lean, exit 1 |
| `field`       | **PANIC**  | —      | —   | 0 | 0    | **#9 charon stack overflow** on `packed_ext_powers_capped_ty` |
| `air`         | **PANIC**  | —      | —   | 0 | 0    | #9 — aborts compiling **`p3-air` itself** (289 GAT warns) |
| `commit`      | **PANIC**  | —      | —   | 0 | 0    | #9 — aborts compiling **`p3-commit` itself** |
| `monty-31`    | OK(9 type errors) | 773 | 768 | 0 | 0 | #6 cascade + #16 → #11 `CFailure` (B) aborts before Lean |
| `mds`         | OK         | 94     | 91  | 0 | 0    | #6 cascade → #11 `CFailure` (B) aborts before Lean |
| `symmetric`   | OK(**49** type errors) | 134 | 127 | 0 | 0 | #6 cascade + **#16 (worst hit)** → #11 `CFailure` (C) before Lean |
| `merkle-tree` | OK(8 type errors) | 419 | 412 | 0 | 0 | #6 cascade + #12 HRTB (36) + #16 → #11 `CFailure` (A) before Lean |

Rows ordered best-to-worst. `OK(N type errors)` = charon exits 0 and writes the
`.llbc` while reporting N `error: Type error after transformations` (#16).
GAT-cascade warnings (`Could not compute the value of`) also frozen: `air` 289,
`merkle-tree` 99, `commit` 43, `monty-31` 27, `symmetric` 18, `field`/`mds` 17,
`koala-bear` 6, `poseidon2`/`challenger` 4, zero in `util`/`keccak`/`maybe-rayon`.

## Obstruction catalog

Numbering carried over from 2026-08-06. **Nothing new, nothing resolved, nothing
moved.** Every entry below reproduces the 2026-08-06 evidence byte-for-byte; see
[that report](2026-08-06.md) for full error texts and analysis.

### Issue #9 — charon stack overflow lifting associated types on `p3-field` (top blocker, still unfiled)
Identical: `thread 'main' … has overflowed its stack` → `signal: 6, SIGABRT`
compiling `p3-field` (and `p3-air`/`p3-commit` directly), recursing on
`packed_ext_powers_capped_ty`; same GAT warning counts (17/289/43); still no
backtrace under `RUST_BACKTRACE=1`. Blocks `field`/`air`/`commit`. **Unfiled
since 2026-06-04 — eleven weeks.**

### Issue #16 — charon reports `Type error after transformations` yet exits 0 (unfiled)
Identical: `symmetric` 49, `monty-31` 9, `merkle-tree` 8, `koala-bear` 2 —
charon exit 0, `.llbc` written, aeneas then reports
`Found type error in the output of charon` (4/4/2). The 7-line
RPITIT/`IntoIterator` repro from 2026-08-06 stands (also the minimal reproducer
for #11 sub-path A).

### Issue #6 — mixed mutually-recursive declaration groups (dominant cascade root)
Identical: the `PackedValue` trait × `unpack_iter::closure` type group;
`Translate.ml:392` dominance unchanged (e.g. 749/773 in `monty-31`). Partial
upstream coverage now **three** tickets: aeneas
[#176](https://github.com/AeneasVerif/aeneas/issues/176) (trait × trait),
[#1241](https://github.com/AeneasVerif/aeneas/issues/1241) (documents the
dropped-decl consequence), and — **new this run** —
[#1264](https://github.com/AeneasVerif/aeneas/issues/1264) (fun × trait-impl,
filed with two small repros).

### Issue #11 — aeneas uncaught `CFailure` → aeneas#1164 (OPEN)
Identical: exit 2, no Lean, five crates (`util`, `monty-31`, `mds`,
`symmetric`, `merkle-tree`); sub-paths A/B/C at unchanged line numbers
(`translate_fwd_ty:396-398`, `TypesAnalysis.ml:372`,
`translate_fun_sigs:1012-1013`).

### Issue #12 — aeneas HRTB lifetime constraint → aeneas#1227 (OPEN)
Identical: `merkle-tree` 36, `challenger` 8. Gated on charon
[#1143](https://github.com/AeneasVerif/charon/issues/1143).

### Issue #13 — "arrow types with locally quantified regions" (unfiled)
Identical: `poseidon2` 6, `monty-31` 2. One-line repro
(`pub fn arrow_elided(_g: fn(&u32)) {}`) unchanged.

### Issue #14 — `Could not find: type_var_id: 0 from ExtractBase.Item` (keccak's last error, unfiled)
Identical: 2 occurrences / 1 unique at `keccak/src/lib.rs:121:8-123:9` — still
the only thing between `keccak` and a clean extraction.

### Issue #3 — GATs / associated types with parameters → aeneas#1112
Identical: `SymbolicToPure.ml:222` fires 4 (`koala-bear`), 4 (`poseidon2`),
3 (`challenger`), killing `RawDataSerializable` and `PackedValue`.

### Issue #7 — `Not_found` from `trait_impl_is_builtin` → aeneas#1102 (OPEN)
Identical: `Translate.ml:1073-1074`, aborts `koala-bear` after 1898 lines.

### Issue #8 — closures / lambdas don't extract → aeneas#1119 etc.
Identical: `SymbolicToPure.ml:146` — 6/7/7/18/19/20 across the six affected
crates.

### Issue #15 — missing Lean `Iterator` builtin models (minor)
Identical: 5 warns in clean `maybe-rayon`, 5 in `poseidon2`, 4 in `challenger`.
(New aeneas [#1259](https://github.com/AeneasVerif/aeneas/issues/1259) —
missing `From<bool>` instances — is the same "Lean stdlib gaps" family, a
different instance.)

### Issue #4 — raw pointers / inline asm (`util`, `monty-31`) — unchanged known limitation
### Issue #5 — stdlib name clashes — still not observed (`util` dies earlier on #11)
### Issue #10 — `ty_has_mut_borrow_for_region_in_pred` — still not reproducing; aeneas#1239 OPEN
### Issue #2 — parent-clause/const/type lookups — still gone (since 2026-06-16)
### Issue #1 — charon `GenericsMismatch` panic — RESOLVED (see [RESOLVED.md](RESOLVED.md))

## Upstream cross-reference (2026-08-18)

All states re-verified via `gh` this run. Only movement vs 2026-08-06: aeneas
#1264 added as a partial match on #6.

| Local issue | Status upstream | Tickets |
| --- | --- | --- |
| #9 charon stack overflow (assoc-type lift) | **Not represented — file it** | none (generic tracking charon [#145](https://github.com/AeneasVerif/charon/issues/145) OPEN) |
| #16 charon ill-typed LLBC + exit 0 | **Not represented — file it** | none |
| #13 aeneas "arrow types with locally quantified regions" | **Not represented — file it** | none |
| #14 aeneas `Could not find: type_var_id: 0` | **Not represented — file it** | none |
| #6 mixed mutually-recursive groups (trait × closure type) | **Partial** — three adjacent tickets, none our flavor | aeneas [#176](https://github.com/AeneasVerif/aeneas/issues/176), [#1241](https://github.com/AeneasVerif/aeneas/issues/1241), [#1264](https://github.com/AeneasVerif/aeneas/issues/1264) (**new**) — all OPEN |
| #3 GATs / assoc types with parameters | Represented | aeneas [#1112](https://github.com/AeneasVerif/aeneas/issues/1112) OPEN; charon [#1078](https://github.com/AeneasVerif/charon/issues/1078), [#534](https://github.com/AeneasVerif/charon/issues/534) OPEN |
| `Could not find: trait_decl_id: N` (downstream of #6) | Represented | aeneas [#1241](https://github.com/AeneasVerif/aeneas/issues/1241) OPEN |
| #11 aeneas uncaught `CFailure` in method-sig translation | Represented | aeneas [#1164](https://github.com/AeneasVerif/aeneas/issues/1164) OPEN |
| #12 aeneas HRTB lifetime constraint | Represented | aeneas [#1227](https://github.com/AeneasVerif/aeneas/issues/1227) OPEN, gated on charon [#1143](https://github.com/AeneasVerif/charon/issues/1143) OPEN |
| #8 closures fail to extract | Represented | aeneas [#1119](https://github.com/AeneasVerif/aeneas/issues/1119), [#924](https://github.com/AeneasVerif/aeneas/issues/924), [#804](https://github.com/AeneasVerif/aeneas/issues/804) OPEN |
| #7 `Not_found` in `trait_impl_is_builtin` | Represented (ours, OPEN) | aeneas [#1102](https://github.com/AeneasVerif/aeneas/issues/1102) |
| #15 missing Lean `Iterator` builtin models | Partial | aeneas [#1043](https://github.com/AeneasVerif/aeneas/issues/1043), [#1132](https://github.com/AeneasVerif/aeneas/issues/1132) OPEN; adjacent: [#1259](https://github.com/AeneasVerif/aeneas/issues/1259) |
| #10 aeneas `Not_found` in `ty_has_mut_borrow…` | Represented, not reproducing here | aeneas [#1239](https://github.com/AeneasVerif/aeneas/issues/1239) OPEN |
| #5 stdlib name clashes (not observed) | Represented | aeneas [#922](https://github.com/AeneasVerif/aeneas/issues/922), [#1046](https://github.com/AeneasVerif/aeneas/issues/1046) OPEN |
| #4 raw pointers / asm / ptr casts | Known limitation | (charon [#1041](https://github.com/AeneasVerif/charon/issues/1041) CLOSED, loose match only) |
| #2 parent-clause/const/type lookups | Cascade gone; root NOT_PLANNED | charon [#1266](https://github.com/AeneasVerif/charon/issues/1266) CLOSED |
| #1 charon `GenericsMismatch` panic | RESOLVED | charon [#1260](https://github.com/AeneasVerif/charon/issues/1260) / [#1263](https://github.com/AeneasVerif/charon/pull/1263) |

Still relevant to workarounds: charon
[#1339](https://github.com/AeneasVerif/charon/issues/1339) OPEN —
`--opaque`/`--exclude` cannot target closure sub-items (see
[`WORKAROUNDS.md`](WORKAROUNDS.md)).

## Obstructions with no upstream issue/PR

The same four as 2026-08-06 — all re-verified byte-for-byte this run, all with
working reproducers, all drafts regenerated in `issue-drafts/` (gitignored):

1. **charon: stack overflow lifting associated types** (Issue #9). #1 blocker;
   kills `field`/`air`/`commit`; **eleven weeks unfiled**. Draft:
   `issue-drafts/01-charon-stack-overflow-assoc-type-lift.md`. **File on charon.**
2. **charon: `Type error after transformations` with exit 0 → ill-typed LLBC**
   (Issue #16). 68 occurrences across four crates; 7-line dependency-free repro
   that also reproduces aeneas #1164. Draft:
   `issue-drafts/02-charon-type-error-after-transformations-exit-zero.md`.
   **File on charon.**
3. **aeneas: arrow types with locally quantified regions** (Issue #13).
   One-line repro. Draft:
   `issue-drafts/03-aeneas-arrow-types-locally-quantified-regions.md`.
   **File on aeneas.**
4. **aeneas: `Could not find: type_var_id: 0` in extraction** (Issue #14). The
   last error in `keccak`. One-line repro. Draft:
   `issue-drafts/04-aeneas-type-var-id-not-found-extraction.md`.
   **File on aeneas.**

## Recommendation

**Nothing changed, which is itself the finding: the survey is now a proven
deterministic baseline** (identical toolchain + identical source → identical
output, down to OCaml line numbers), and every day of "no movement" is now
attributable purely to upstream inactivity on our obstructions — of which there
was none in this window, despite a dozen new third-party aeneas reports landing
in the same twelve days.

The action items are unchanged from 2026-08-06 and grow more overdue with each
null run — **the bottleneck is filing, not surveying**:

1. **File #16 and #9 on charon, #13 and #14 on aeneas.** All four drafts are
   regenerated and current; the evidence has now survived three toolchain
   states (#9) or two identical runs (#16). Nobody upstream has independently
   hit them in twelve weeks of tracker traffic.
2. **Comment on aeneas #176 with our #6 group** — the case is again cheaper:
   #1264 is now a *third* corroborating report of the mixed-group limitation,
   and ours (trait × closure-type) is still the only flavor not on record.
3. **Add the #16 7-line repro to aeneas #1164** (our #11) — still the
   difference between five crates emitting nothing vs partial Lean.
4. **Still overdue: `lake build` the `maybe-rayon` and `keccak` output** to
   learn whether "0 errors" means "usable Lean" (#15 and aeneas #1242 suggest
   maybe not).
5. If the toolchain is still frozen at `daa85d7e`/`340b1af4` next run, consider
   lengthening the survey interval and spending the time on the filings above
   instead.

The hax workspace at `koala-bear/proofs/lean/extraction/` remains the working
verification target.
