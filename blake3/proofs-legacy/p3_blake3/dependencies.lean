-- Hand-written dependency stubs imported by the extracted `p3_blake3.lean`.
-- (The extraction's `import Hax` line is rewritten to import this module by
-- ../build-lean.sh — see ../README.md.)
import p3_blake3.core_shims
import p3_blake3.blake3
import p3_blake3.p3_util
-- p3_symmetric's trait classes now come from the real extracted package
-- (see lakefile.toml's path require), not a local stub.
import p3_symmetric
