-- Imported by the sliced `p3_symmetric.lean` (the trait-class interface only).
-- The sliced classes depend solely on `core_models`/Hax, so there are no stubs
-- to pull in here. (Kept as the stable import target for build-lean.sh.)
import Hax
